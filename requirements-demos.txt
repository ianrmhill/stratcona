# Link the base Stratcona dependencies
-r requirements.txt

# Required for testing the package
pytest >= 7.3.1

# Required for optional demos
matplotlib >= 3.10.0
seaborn >= 0.13.2
reliability >= 0.8.16